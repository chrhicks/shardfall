# Continuum (Agent Guide)

Continuum is an agent-first task system that stores work context so tasks can be resumed across sessions.

To begin, call `continuum_init` before any other tool.

# GitHub Searching

Use the `eugene` command to search public GitHub repositories and understand code/implementation for dependencies of this application. Run `eugene prompt`. This does not work for private repositories or providers outside of GitHub.